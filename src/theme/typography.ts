export const casinoTypography = {
  fontFamily: '"Rajdhani", "Inter", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
  monoFamily: '"Roboto Mono", "SFMono-Regular", Consolas, monospace',
  headingWeight: 900,
  labelWeight: 800,
  numberWeight: 900,
} as const;

export const casinoTypeScale = {
  hero: "clamp(2.35rem, 9vw, 5.25rem)",
  h1: "clamp(2rem, 6vw, 3.6rem)",
  h2: "clamp(1.25rem, 3vw, 1.8rem)",
  body: "clamp(0.92rem, 2.4vw, 1rem)",
  label: "0.78rem",
} as const;
