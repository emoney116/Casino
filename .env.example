# This prototype falls back to localStorage when these are omitted.
# Add Supabase values only when testing the optional backend mode.
# No payment processor, cashout, redemption, or sweepstakes service is configured.

VITE_APP_NAME="Casino Prototype"
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-supabase-anon-key"
